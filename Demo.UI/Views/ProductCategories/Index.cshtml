@{
    ViewBag.Title = "Product Categories";
}

<table id="myTable"></table>
<script>
    $(document).ready(function () {
        $('#myTable').dataTable({
            serverSide: true,
            processing: true,
            paging: false,
            info: false,
            ajax: {
                url: "http://localhost/SqlApi/api/ProductCategories",
                dataSrc : ''
            },
            columns: [
                {
                    name: 'Name',
                    data: "Name",
                    title: "Name",
                    sortable: false,
                    searchable: false
                }
                //,
                //{
                //   render: function (data, type, full, meta) {
                //       return 'dupa ' + full.Name;
                //    }
                //}
            ]
        });
    });

    function renderNameColumn(id) {
        return 'dupa ' + id;
    }
</script>